const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d // contem 2 operadores, são chamados de operadores binarios.
const subtracao = d - b
const multiplicaçao = a * b
const divisao = d / a
const modulo = a % 2 // se a for um numero par a divisão vai ser 0, e se A for impa o modulo sempre séra impa

console.log(soma, subtracao, multiplicaçao, divisao, modulo)
